import 'package:flutter/material.dart';

class SpeedGameScreen extends StatelessWidget {
  static const routeName = '/game/speed';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Speed game screen"),
    );
  }
}
