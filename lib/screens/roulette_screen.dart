import 'package:flutter/material.dart';

class RouletteScreen extends StatelessWidget {
  static const routeName = '/roulette';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Roulette screen"),
    );
  }
}
