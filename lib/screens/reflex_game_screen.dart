import 'package:flutter/material.dart';

class ReflexGameScreen extends StatelessWidget {
  static const routeName = '/game/reflex';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Reflex game screen"),
    );
  }
}
